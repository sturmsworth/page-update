import React from "react";

const FormsContainer = ({ content }) => (
  <section className="forms-container container-fluid text-center">
    {content}
  </section>
);

export default FormsContainer;
